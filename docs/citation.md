# Citation

Use the repository-root `CITATION.cff` metadata when citing TemporalFix. The
working paper title is *TemporalFix: Detector-Agnostic Temporal Repair and
Uncertainty Estimation for Video Object Detection*.

No DOI is claimed until a release archive has actually been created. Citation
metadata version must match the package version for every release.
